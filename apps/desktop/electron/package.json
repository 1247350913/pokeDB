{
  "name": "@apps/desktop-electron",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsc -b && cross-env VITE_DEV_SERVER_URL=http://localhost:5173 electronmon .",
    "build": "tsc -b",
    "start": "electron .",
    "typecheck": "tsc -b --noEmit"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "electron": "^31.7.7",
    "electronmon": "^2.0.3",
    "typescript": "^5.6.3"
  }
}